{
  "name": "my-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-scripts": "1.0.14",
    "serve": "^11.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "lintme": "echo 'Hello is it me you looking for...'",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "devEnt": "npm install && npm run build",
    "serve": "npm i && npm run build && serve -s build"
  }
}
